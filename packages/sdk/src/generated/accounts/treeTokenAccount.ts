/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const TREE_TOKEN_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([153, 63, 39, 198, 74, 80, 37, 204]);

export function getTreeTokenAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TREE_TOKEN_ACCOUNT_DISCRIMINATOR,
  );
}

export type TreeTokenAccount = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  bump: number;
};

export type TreeTokenAccountArgs = { authority: Address; bump: number };

/** Gets the encoder for {@link TreeTokenAccountArgs} account data. */
export function getTreeTokenAccountEncoder(): FixedSizeEncoder<TreeTokenAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TREE_TOKEN_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TreeTokenAccount} account data. */
export function getTreeTokenAccountDecoder(): FixedSizeDecoder<TreeTokenAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link TreeTokenAccount} account data. */
export function getTreeTokenAccountCodec(): FixedSizeCodec<
  TreeTokenAccountArgs,
  TreeTokenAccount
> {
  return combineCodec(
    getTreeTokenAccountEncoder(),
    getTreeTokenAccountDecoder(),
  );
}

export function decodeTreeTokenAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TreeTokenAccount, TAddress>;
export function decodeTreeTokenAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TreeTokenAccount, TAddress>;
export function decodeTreeTokenAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<TreeTokenAccount, TAddress>
  | MaybeAccount<TreeTokenAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTreeTokenAccountDecoder(),
  );
}

export async function fetchTreeTokenAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TreeTokenAccount, TAddress>> {
  const maybeAccount = await fetchMaybeTreeTokenAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTreeTokenAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TreeTokenAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTreeTokenAccount(maybeAccount);
}

export async function fetchAllTreeTokenAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TreeTokenAccount>[]> {
  const maybeAccounts = await fetchAllMaybeTreeTokenAccount(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTreeTokenAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TreeTokenAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeTreeTokenAccount(maybeAccount),
  );
}

export function getTreeTokenAccountSize(): number {
  return 41;
}
