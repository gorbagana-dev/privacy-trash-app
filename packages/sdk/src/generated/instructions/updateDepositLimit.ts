/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findTreeAccountPda } from "../pdas";
import { ZKCASH_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_DEPOSIT_LIMIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([181, 115, 65, 169, 4, 1, 96, 109]);

export function getUpdateDepositLimitDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_DEPOSIT_LIMIT_DISCRIMINATOR,
  );
}

export type UpdateDepositLimitInstruction<
  TProgram extends string = typeof ZKCASH_PROGRAM_ADDRESS,
  TAccountTreeAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTreeAccount extends string
        ? WritableAccount<TAccountTreeAccount>
        : TAccountTreeAccount,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateDepositLimitInstructionData = {
  discriminator: ReadonlyUint8Array;
  newLimit: bigint;
};

export type UpdateDepositLimitInstructionDataArgs = {
  newLimit: number | bigint;
};

export function getUpdateDepositLimitInstructionDataEncoder(): FixedSizeEncoder<UpdateDepositLimitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newLimit", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_DEPOSIT_LIMIT_DISCRIMINATOR,
    }),
  );
}

export function getUpdateDepositLimitInstructionDataDecoder(): FixedSizeDecoder<UpdateDepositLimitInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newLimit", getU64Decoder()],
  ]);
}

export function getUpdateDepositLimitInstructionDataCodec(): FixedSizeCodec<
  UpdateDepositLimitInstructionDataArgs,
  UpdateDepositLimitInstructionData
> {
  return combineCodec(
    getUpdateDepositLimitInstructionDataEncoder(),
    getUpdateDepositLimitInstructionDataDecoder(),
  );
}

export type UpdateDepositLimitAsyncInput<
  TAccountTreeAccount extends string = string,
  TAccountAuthority extends string = string,
> = {
  treeAccount?: Address<TAccountTreeAccount>;
  /** The authority account that can update the deposit limit */
  authority: TransactionSigner<TAccountAuthority>;
  newLimit: UpdateDepositLimitInstructionDataArgs["newLimit"];
};

export async function getUpdateDepositLimitInstructionAsync<
  TAccountTreeAccount extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ZKCASH_PROGRAM_ADDRESS,
>(
  input: UpdateDepositLimitAsyncInput<TAccountTreeAccount, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateDepositLimitInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZKCASH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    treeAccount: { value: input.treeAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.treeAccount.value) {
    accounts.treeAccount.value = await findTreeAccountPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("treeAccount", accounts.treeAccount),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateDepositLimitInstructionDataEncoder().encode(
      args as UpdateDepositLimitInstructionDataArgs,
    ),
    programAddress,
  } as UpdateDepositLimitInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountAuthority
  >);
}

export type UpdateDepositLimitInput<
  TAccountTreeAccount extends string = string,
  TAccountAuthority extends string = string,
> = {
  treeAccount: Address<TAccountTreeAccount>;
  /** The authority account that can update the deposit limit */
  authority: TransactionSigner<TAccountAuthority>;
  newLimit: UpdateDepositLimitInstructionDataArgs["newLimit"];
};

export function getUpdateDepositLimitInstruction<
  TAccountTreeAccount extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ZKCASH_PROGRAM_ADDRESS,
>(
  input: UpdateDepositLimitInput<TAccountTreeAccount, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateDepositLimitInstruction<
  TProgramAddress,
  TAccountTreeAccount,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZKCASH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    treeAccount: { value: input.treeAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("treeAccount", accounts.treeAccount),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateDepositLimitInstructionDataEncoder().encode(
      args as UpdateDepositLimitInstructionDataArgs,
    ),
    programAddress,
  } as UpdateDepositLimitInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountAuthority
  >);
}

export type ParsedUpdateDepositLimitInstruction<
  TProgram extends string = typeof ZKCASH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    treeAccount: TAccountMetas[0];
    /** The authority account that can update the deposit limit */
    authority: TAccountMetas[1];
  };
  data: UpdateDepositLimitInstructionData;
};

export function parseUpdateDepositLimitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateDepositLimitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { treeAccount: getNextAccount(), authority: getNextAccount() },
    data: getUpdateDepositLimitInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
