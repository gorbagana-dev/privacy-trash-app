/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findGlobalConfigPda,
  findTreeAccountPda,
  findTreeTokenAccountPda,
} from "../pdas";
import { ZKCASH_PROGRAM_ADDRESS } from "../programs";
import {
  getExtDataMinifiedDecoder,
  getExtDataMinifiedEncoder,
  getProofDecoder,
  getProofEncoder,
  type ExtDataMinified,
  type ExtDataMinifiedArgs,
  type Proof,
  type ProofArgs,
} from "../types";

export const TRANSACT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  217, 149, 130, 143, 221, 52, 252, 119,
]);

export function getTransactDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TRANSACT_DISCRIMINATOR);
}

export type TransactInstruction<
  TProgram extends string = typeof ZKCASH_PROGRAM_ADDRESS,
  TAccountTreeAccount extends string | AccountMeta<string> = string,
  TAccountNullifier0 extends string | AccountMeta<string> = string,
  TAccountNullifier1 extends string | AccountMeta<string> = string,
  TAccountNullifier2 extends string | AccountMeta<string> = string,
  TAccountNullifier3 extends string | AccountMeta<string> = string,
  TAccountTreeTokenAccount extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountFeeRecipientAccount extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTreeAccount extends string
        ? WritableAccount<TAccountTreeAccount>
        : TAccountTreeAccount,
      TAccountNullifier0 extends string
        ? WritableAccount<TAccountNullifier0>
        : TAccountNullifier0,
      TAccountNullifier1 extends string
        ? WritableAccount<TAccountNullifier1>
        : TAccountNullifier1,
      TAccountNullifier2 extends string
        ? ReadonlyAccount<TAccountNullifier2>
        : TAccountNullifier2,
      TAccountNullifier3 extends string
        ? ReadonlyAccount<TAccountNullifier3>
        : TAccountNullifier3,
      TAccountTreeTokenAccount extends string
        ? WritableAccount<TAccountTreeTokenAccount>
        : TAccountTreeTokenAccount,
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountFeeRecipientAccount extends string
        ? WritableAccount<TAccountFeeRecipientAccount>
        : TAccountFeeRecipientAccount,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TransactInstructionData = {
  discriminator: ReadonlyUint8Array;
  proof: Proof;
  extDataMinified: ExtDataMinified;
  encryptedOutput1: ReadonlyUint8Array;
  encryptedOutput2: ReadonlyUint8Array;
};

export type TransactInstructionDataArgs = {
  proof: ProofArgs;
  extDataMinified: ExtDataMinifiedArgs;
  encryptedOutput1: ReadonlyUint8Array;
  encryptedOutput2: ReadonlyUint8Array;
};

export function getTransactInstructionDataEncoder(): Encoder<TransactInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["proof", getProofEncoder()],
      ["extDataMinified", getExtDataMinifiedEncoder()],
      [
        "encryptedOutput1",
        addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
      ],
      [
        "encryptedOutput2",
        addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: TRANSACT_DISCRIMINATOR }),
  );
}

export function getTransactInstructionDataDecoder(): Decoder<TransactInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["proof", getProofDecoder()],
    ["extDataMinified", getExtDataMinifiedDecoder()],
    [
      "encryptedOutput1",
      addDecoderSizePrefix(getBytesDecoder(), getU32Decoder()),
    ],
    [
      "encryptedOutput2",
      addDecoderSizePrefix(getBytesDecoder(), getU32Decoder()),
    ],
  ]);
}

export function getTransactInstructionDataCodec(): Codec<
  TransactInstructionDataArgs,
  TransactInstructionData
> {
  return combineCodec(
    getTransactInstructionDataEncoder(),
    getTransactInstructionDataDecoder(),
  );
}

export type TransactAsyncInput<
  TAccountTreeAccount extends string = string,
  TAccountNullifier0 extends string = string,
  TAccountNullifier1 extends string = string,
  TAccountNullifier2 extends string = string,
  TAccountNullifier3 extends string = string,
  TAccountTreeTokenAccount extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountRecipient extends string = string,
  TAccountFeeRecipientAccount extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  treeAccount?: Address<TAccountTreeAccount>;
  /**
   * Nullifier account to mark the first input as spent.
   * Using `init` without `init_if_needed` ensures that the transaction
   * will automatically fail with a system program error if this nullifier
   * has already been used (i.e., if the account already exists).
   */
  nullifier0: Address<TAccountNullifier0>;
  /**
   * Nullifier account to mark the second input as spent.
   * Using `init` without `init_if_needed` ensures that the transaction
   * will automatically fail with a system program error if this nullifier
   * has already been used (i.e., if the account already exists).
   */
  nullifier1: Address<TAccountNullifier1>;
  nullifier2: Address<TAccountNullifier2>;
  nullifier3: Address<TAccountNullifier3>;
  treeTokenAccount?: Address<TAccountTreeTokenAccount>;
  globalConfig?: Address<TAccountGlobalConfig>;
  recipient: Address<TAccountRecipient>;
  feeRecipientAccount: Address<TAccountFeeRecipientAccount>;
  /** The account that is signing the transaction */
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  proof: TransactInstructionDataArgs["proof"];
  extDataMinified: TransactInstructionDataArgs["extDataMinified"];
  encryptedOutput1: TransactInstructionDataArgs["encryptedOutput1"];
  encryptedOutput2: TransactInstructionDataArgs["encryptedOutput2"];
};

export async function getTransactInstructionAsync<
  TAccountTreeAccount extends string,
  TAccountNullifier0 extends string,
  TAccountNullifier1 extends string,
  TAccountNullifier2 extends string,
  TAccountNullifier3 extends string,
  TAccountTreeTokenAccount extends string,
  TAccountGlobalConfig extends string,
  TAccountRecipient extends string,
  TAccountFeeRecipientAccount extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZKCASH_PROGRAM_ADDRESS,
>(
  input: TransactAsyncInput<
    TAccountTreeAccount,
    TAccountNullifier0,
    TAccountNullifier1,
    TAccountNullifier2,
    TAccountNullifier3,
    TAccountTreeTokenAccount,
    TAccountGlobalConfig,
    TAccountRecipient,
    TAccountFeeRecipientAccount,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TransactInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountNullifier0,
    TAccountNullifier1,
    TAccountNullifier2,
    TAccountNullifier3,
    TAccountTreeTokenAccount,
    TAccountGlobalConfig,
    TAccountRecipient,
    TAccountFeeRecipientAccount,
    TAccountSigner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZKCASH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    treeAccount: { value: input.treeAccount ?? null, isWritable: true },
    nullifier0: { value: input.nullifier0 ?? null, isWritable: true },
    nullifier1: { value: input.nullifier1 ?? null, isWritable: true },
    nullifier2: { value: input.nullifier2 ?? null, isWritable: false },
    nullifier3: { value: input.nullifier3 ?? null, isWritable: false },
    treeTokenAccount: {
      value: input.treeTokenAccount ?? null,
      isWritable: true,
    },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: true },
    feeRecipientAccount: {
      value: input.feeRecipientAccount ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.treeAccount.value) {
    accounts.treeAccount.value = await findTreeAccountPda();
  }
  if (!accounts.treeTokenAccount.value) {
    accounts.treeTokenAccount.value = await findTreeTokenAccountPda();
  }
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("treeAccount", accounts.treeAccount),
      getAccountMeta("nullifier0", accounts.nullifier0),
      getAccountMeta("nullifier1", accounts.nullifier1),
      getAccountMeta("nullifier2", accounts.nullifier2),
      getAccountMeta("nullifier3", accounts.nullifier3),
      getAccountMeta("treeTokenAccount", accounts.treeTokenAccount),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("feeRecipientAccount", accounts.feeRecipientAccount),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getTransactInstructionDataEncoder().encode(
      args as TransactInstructionDataArgs,
    ),
    programAddress,
  } as TransactInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountNullifier0,
    TAccountNullifier1,
    TAccountNullifier2,
    TAccountNullifier3,
    TAccountTreeTokenAccount,
    TAccountGlobalConfig,
    TAccountRecipient,
    TAccountFeeRecipientAccount,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type TransactInput<
  TAccountTreeAccount extends string = string,
  TAccountNullifier0 extends string = string,
  TAccountNullifier1 extends string = string,
  TAccountNullifier2 extends string = string,
  TAccountNullifier3 extends string = string,
  TAccountTreeTokenAccount extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountRecipient extends string = string,
  TAccountFeeRecipientAccount extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  treeAccount: Address<TAccountTreeAccount>;
  /**
   * Nullifier account to mark the first input as spent.
   * Using `init` without `init_if_needed` ensures that the transaction
   * will automatically fail with a system program error if this nullifier
   * has already been used (i.e., if the account already exists).
   */
  nullifier0: Address<TAccountNullifier0>;
  /**
   * Nullifier account to mark the second input as spent.
   * Using `init` without `init_if_needed` ensures that the transaction
   * will automatically fail with a system program error if this nullifier
   * has already been used (i.e., if the account already exists).
   */
  nullifier1: Address<TAccountNullifier1>;
  nullifier2: Address<TAccountNullifier2>;
  nullifier3: Address<TAccountNullifier3>;
  treeTokenAccount: Address<TAccountTreeTokenAccount>;
  globalConfig: Address<TAccountGlobalConfig>;
  recipient: Address<TAccountRecipient>;
  feeRecipientAccount: Address<TAccountFeeRecipientAccount>;
  /** The account that is signing the transaction */
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  proof: TransactInstructionDataArgs["proof"];
  extDataMinified: TransactInstructionDataArgs["extDataMinified"];
  encryptedOutput1: TransactInstructionDataArgs["encryptedOutput1"];
  encryptedOutput2: TransactInstructionDataArgs["encryptedOutput2"];
};

export function getTransactInstruction<
  TAccountTreeAccount extends string,
  TAccountNullifier0 extends string,
  TAccountNullifier1 extends string,
  TAccountNullifier2 extends string,
  TAccountNullifier3 extends string,
  TAccountTreeTokenAccount extends string,
  TAccountGlobalConfig extends string,
  TAccountRecipient extends string,
  TAccountFeeRecipientAccount extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZKCASH_PROGRAM_ADDRESS,
>(
  input: TransactInput<
    TAccountTreeAccount,
    TAccountNullifier0,
    TAccountNullifier1,
    TAccountNullifier2,
    TAccountNullifier3,
    TAccountTreeTokenAccount,
    TAccountGlobalConfig,
    TAccountRecipient,
    TAccountFeeRecipientAccount,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): TransactInstruction<
  TProgramAddress,
  TAccountTreeAccount,
  TAccountNullifier0,
  TAccountNullifier1,
  TAccountNullifier2,
  TAccountNullifier3,
  TAccountTreeTokenAccount,
  TAccountGlobalConfig,
  TAccountRecipient,
  TAccountFeeRecipientAccount,
  TAccountSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZKCASH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    treeAccount: { value: input.treeAccount ?? null, isWritable: true },
    nullifier0: { value: input.nullifier0 ?? null, isWritable: true },
    nullifier1: { value: input.nullifier1 ?? null, isWritable: true },
    nullifier2: { value: input.nullifier2 ?? null, isWritable: false },
    nullifier3: { value: input.nullifier3 ?? null, isWritable: false },
    treeTokenAccount: {
      value: input.treeTokenAccount ?? null,
      isWritable: true,
    },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: true },
    feeRecipientAccount: {
      value: input.feeRecipientAccount ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("treeAccount", accounts.treeAccount),
      getAccountMeta("nullifier0", accounts.nullifier0),
      getAccountMeta("nullifier1", accounts.nullifier1),
      getAccountMeta("nullifier2", accounts.nullifier2),
      getAccountMeta("nullifier3", accounts.nullifier3),
      getAccountMeta("treeTokenAccount", accounts.treeTokenAccount),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("feeRecipientAccount", accounts.feeRecipientAccount),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getTransactInstructionDataEncoder().encode(
      args as TransactInstructionDataArgs,
    ),
    programAddress,
  } as TransactInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountNullifier0,
    TAccountNullifier1,
    TAccountNullifier2,
    TAccountNullifier3,
    TAccountTreeTokenAccount,
    TAccountGlobalConfig,
    TAccountRecipient,
    TAccountFeeRecipientAccount,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type ParsedTransactInstruction<
  TProgram extends string = typeof ZKCASH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    treeAccount: TAccountMetas[0];
    /**
     * Nullifier account to mark the first input as spent.
     * Using `init` without `init_if_needed` ensures that the transaction
     * will automatically fail with a system program error if this nullifier
     * has already been used (i.e., if the account already exists).
     */
    nullifier0: TAccountMetas[1];
    /**
     * Nullifier account to mark the second input as spent.
     * Using `init` without `init_if_needed` ensures that the transaction
     * will automatically fail with a system program error if this nullifier
     * has already been used (i.e., if the account already exists).
     */
    nullifier1: TAccountMetas[2];
    nullifier2: TAccountMetas[3];
    nullifier3: TAccountMetas[4];
    treeTokenAccount: TAccountMetas[5];
    globalConfig: TAccountMetas[6];
    recipient: TAccountMetas[7];
    feeRecipientAccount: TAccountMetas[8];
    /** The account that is signing the transaction */
    signer: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
  };
  data: TransactInstructionData;
};

export function parseTransactInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTransactInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 11,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      treeAccount: getNextAccount(),
      nullifier0: getNextAccount(),
      nullifier1: getNextAccount(),
      nullifier2: getNextAccount(),
      nullifier3: getNextAccount(),
      treeTokenAccount: getNextAccount(),
      globalConfig: getNextAccount(),
      recipient: getNextAccount(),
      feeRecipientAccount: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getTransactInstructionDataDecoder().decode(instruction.data),
  };
}
