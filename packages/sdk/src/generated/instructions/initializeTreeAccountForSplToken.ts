/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findGlobalConfigPda,
  findInitializeTreeAccountForSplTokenTreeAccountPda,
} from "../pdas";
import { ZKCASH_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_TREE_ACCOUNT_FOR_SPL_TOKEN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([19, 59, 201, 78, 69, 86, 50, 209]);

export function getInitializeTreeAccountForSplTokenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_TREE_ACCOUNT_FOR_SPL_TOKEN_DISCRIMINATOR,
  );
}

export type InitializeTreeAccountForSplTokenInstruction<
  TProgram extends string = typeof ZKCASH_PROGRAM_ADDRESS,
  TAccountTreeAccount extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTreeAccount extends string
        ? WritableAccount<TAccountTreeAccount>
        : TAccountTreeAccount,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeTreeAccountForSplTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  maxDepositAmount: bigint;
};

export type InitializeTreeAccountForSplTokenInstructionDataArgs = {
  maxDepositAmount: number | bigint;
};

export function getInitializeTreeAccountForSplTokenInstructionDataEncoder(): FixedSizeEncoder<InitializeTreeAccountForSplTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["maxDepositAmount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_TREE_ACCOUNT_FOR_SPL_TOKEN_DISCRIMINATOR,
    }),
  );
}

export function getInitializeTreeAccountForSplTokenInstructionDataDecoder(): FixedSizeDecoder<InitializeTreeAccountForSplTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["maxDepositAmount", getU64Decoder()],
  ]);
}

export function getInitializeTreeAccountForSplTokenInstructionDataCodec(): FixedSizeCodec<
  InitializeTreeAccountForSplTokenInstructionDataArgs,
  InitializeTreeAccountForSplTokenInstructionData
> {
  return combineCodec(
    getInitializeTreeAccountForSplTokenInstructionDataEncoder(),
    getInitializeTreeAccountForSplTokenInstructionDataDecoder(),
  );
}

export type InitializeTreeAccountForSplTokenAsyncInput<
  TAccountTreeAccount extends string = string,
  TAccountMint extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  treeAccount?: Address<TAccountTreeAccount>;
  /** SPL Token mint account */
  mint: Address<TAccountMint>;
  globalConfig?: Address<TAccountGlobalConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  maxDepositAmount: InitializeTreeAccountForSplTokenInstructionDataArgs["maxDepositAmount"];
};

export async function getInitializeTreeAccountForSplTokenInstructionAsync<
  TAccountTreeAccount extends string,
  TAccountMint extends string,
  TAccountGlobalConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZKCASH_PROGRAM_ADDRESS,
>(
  input: InitializeTreeAccountForSplTokenAsyncInput<
    TAccountTreeAccount,
    TAccountMint,
    TAccountGlobalConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeTreeAccountForSplTokenInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountMint,
    TAccountGlobalConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZKCASH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    treeAccount: { value: input.treeAccount ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.treeAccount.value) {
    accounts.treeAccount.value =
      await findInitializeTreeAccountForSplTokenTreeAccountPda({
        mint: getAddressFromResolvedInstructionAccount(
          "mint",
          accounts.mint.value,
        ),
      });
  }
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("treeAccount", accounts.treeAccount),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeTreeAccountForSplTokenInstructionDataEncoder().encode(
      args as InitializeTreeAccountForSplTokenInstructionDataArgs,
    ),
    programAddress,
  } as InitializeTreeAccountForSplTokenInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountMint,
    TAccountGlobalConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeTreeAccountForSplTokenInput<
  TAccountTreeAccount extends string = string,
  TAccountMint extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  treeAccount: Address<TAccountTreeAccount>;
  /** SPL Token mint account */
  mint: Address<TAccountMint>;
  globalConfig: Address<TAccountGlobalConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  maxDepositAmount: InitializeTreeAccountForSplTokenInstructionDataArgs["maxDepositAmount"];
};

export function getInitializeTreeAccountForSplTokenInstruction<
  TAccountTreeAccount extends string,
  TAccountMint extends string,
  TAccountGlobalConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZKCASH_PROGRAM_ADDRESS,
>(
  input: InitializeTreeAccountForSplTokenInput<
    TAccountTreeAccount,
    TAccountMint,
    TAccountGlobalConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeTreeAccountForSplTokenInstruction<
  TProgramAddress,
  TAccountTreeAccount,
  TAccountMint,
  TAccountGlobalConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZKCASH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    treeAccount: { value: input.treeAccount ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("treeAccount", accounts.treeAccount),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeTreeAccountForSplTokenInstructionDataEncoder().encode(
      args as InitializeTreeAccountForSplTokenInstructionDataArgs,
    ),
    programAddress,
  } as InitializeTreeAccountForSplTokenInstruction<
    TProgramAddress,
    TAccountTreeAccount,
    TAccountMint,
    TAccountGlobalConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeTreeAccountForSplTokenInstruction<
  TProgram extends string = typeof ZKCASH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    treeAccount: TAccountMetas[0];
    /** SPL Token mint account */
    mint: TAccountMetas[1];
    globalConfig: TAccountMetas[2];
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeTreeAccountForSplTokenInstructionData;
};

export function parseInitializeTreeAccountForSplTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeTreeAccountForSplTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      treeAccount: getNextAccount(),
      mint: getNextAccount(),
      globalConfig: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeTreeAccountForSplTokenInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
